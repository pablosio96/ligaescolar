extends ../layout.jade
block content
  div(class="col-md-4 col-md-offset-4")
    h1(class="azul-oscuro")= title
    form(method="POST" action="/clasificacion" enctype="urlencoded")
      div(class="form-group funkyradio")
      select(name="id_temporada" class="form-control")
        - each temporada in temporadas
          option= temporada.id_temporada
      br
      label Tipo
      br
      select(name="tipo" class="form-control")  
          option  Nadadores
          option  Clubs
      br      
      input(type="submit" class="btn btn-primary" value="Aceptar")
